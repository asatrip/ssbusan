<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <% boards.forEach(board => { %>
  <url>
    <loc><%= locals.setting.siteDomain %>/<%= board.slugRaw %></loc>
    <lastmod><%= board.datetime %></lastmod>
  </url>
  <% }) %>
</urlset>
